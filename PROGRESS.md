# 进度（每次会话先读这里）

## 里程碑状态
| 里程碑 | 状态 |
|---|---|
| M0 仓库骨架与流水线 | ✅ 完成（PR #1，CI 全绿） |
| M1 核心引擎 | ✅ 完成 |
| M2 完整可玩的经典数独 | ✅ 完成 |
| M3 对角线 + 锯齿 | ✅ 完成 |
| M4 杀手数独 | ✅ 完成 |
| M5 武士数独 | ✅ 完成 |
| M6 打磨与交付 | ✅ 完成（自检报告见下） |
| M7 安卓 APK（可选） | ✅ 工作流完成（调试版已在 GitHub 上构建成功；正式签名需老大添加 Secret） |

## 引擎要点（M1）
- `src/engine/rng.ts` xoshiro128**，种子来自 crypto.getRandomValues。
- `src/engine/geometry.ts` 5 模式统一为“格子 + 单元”；武士共享宫去重。
- 唯一性求解器 A `solver.ts`（位掩码 + 唯余/摒除/笼和剪枝 + MRV）；B `dlx.ts`（舞蹈链，独立实现，仅测试用）。
- 人类技巧求解器 `src/engine/human/`：36 种技巧（含杀手专属 7 种），目录与档位在 `state.ts` 的 `TECHS`。
- 出题 `generate.ts`：终盘 → 挖空（1–4 档边挖边要求“≤目标档可解”）→ 评级 → 不符则重试；生成器函数可分片。
- Worker：`src/engine/gen.worker.ts`；客户端 `src/platform/generator-client.ts`（无 Worker 时主线程 12ms 分片）。

## M2 要点
- 界面：Preact；`src/app/`（store.ts 全局状态、actions.ts 所有动作、nav.ts 返回键/历史栈、screens/*）。
- 盘面：`src/app/components/Board.tsx`（SVG，全模式通用：笼虚线内缩、锯齿粗边、对角线、武士子盘、提示标记）。
- 存档：`src/platform/db.ts`（IndexedDB；每次操作一个事务写入 + games_prev 上一份兜底 + 校验和；数据迁移 v0→v1）。
- 出题：Worker `src/engine/gen.worker.ts`；题池 `src/app/pool.ts`（前台空闲时每模式每档预生成 2 道）。
- 2–4 档出题改为“唯一性挖到极小 → 用 ≤目标档技巧解，卡住补一个正确给定数”，最终仍从头评级、档位必须一致。
- 说明页技巧示例：`scripts/gen-tutorials.ts` 从真实生成的题里抓“该技巧是下一步”的盘面 → `src/app/help/examples.json`。
- 可达档位表：`src/app/levels.ts`（某模式达不到的档位不显示；新模式通过验收后再加入）。

## 最近一次测试（真实数字，2026-09-23）
- 类型检查通过；Vitest 36/36（含存档健壮性 6 项、对局逻辑、分享码、统计、评级校准）。
- Playwright 61 通过 / 2 跳过（性能测试只在 pixel-chromium 跑）；3 个设备配置：iPhone 15 Pro Max(WebKit)、Pixel 7(Chromium)、Galaxy S24(Chromium)，子路径 /openai-quota-watch/。
  覆盖：填数、长按笔记、撤销/重做、三种查错、提示三段式、暂停/自动暂停、多残局、放弃确认、通关+历史+统计、分享码往返、备份跨环境恢复、安卓返回、断网冷启动/开新局/继续、SW 更新后存档不丢且对局中不刷新、快速操作后重开、写入中关页面、200% 缩放、5 种竖屏 + 横屏截图（浅/深色，见 docs/screenshots/）、弹层滚动。
- 经典完整样本（FULL_SAMPLE=1）：

| 档 | 题数 | 生成中位数 ms | 最大 ms | 平均步数 | 平均难度分 | 平均给定数 | 超时 |
|---|---|---|---|---|---|---|---|
| 1 | 200 | 9 | 38 | 53.1 | 69.8 | 27.9 | 0 |
| 2 | 200 | 17 | 160 | 58.8 | 86.1 | 24.9 | 0 |
| 3 | 200 | 49 | 476 | 62.1 | 102.5 | 24.6 | 0 |
| 4 | 50 | 28 | 122 | 62.5 | 123.7 | 25.3 | 0 |
| 5 | 20 | 27 | 94 | 68.1 | 238.4 | 24.6 | 0 |

- 性能（浏览器内主线程，Chromium，16 题/档）：正常 CPU 中位数 6/14/29/31/43 ms，最大 31/63/163/75/102 ms；
  4 倍降速中位数 23/63/126/121/110 ms，最大 43/251/746/261/363 ms（1–3 档 <1s、4 档 <5s 均达标）。
- 对比度（WCAG AA）：浅色/深色所有文字配色 ≥ 5.2:1。
- 评级器校准：维基示例题 → 1 档；Arto Inkala 2012、AI Escargot、Easter Monster 等 → 超出 5 档并拒绝（<100ms）。

## M3 结果（2026-09-23）
- 唯一性求解器 A 增加“区块摒除”传播（单元两两交集），锯齿最坏出题耗时从 1.3s 降到 0.5s（正常 CPU）；DLX 仍是独立实现用于交叉验证。
- 锯齿布局：终盘回溯节点上限 1500，找不到就换布局（有解的布局中位数只需 ~50 个节点）。
- 2–4 档补数后若低于目标档，会继续去给定数直到达到目标档。
- 完整样本（每模式 670 道，全部逐题双求解器 + 技巧求解器 + 指纹验证通过）：

对角线：
| 档 | 题数 | 中位数 ms | 最大 ms | 平均步数 | 平均难度分 | 平均给定数 |
|---|---|---|---|---|---|---|
| 1 | 200 | 13 | 97 | 58.6 | 77.6 | 22.4 |
| 2 | 200 | 20 | 149 | 63.6 | 94.7 | 20.9 |
| 3 | 200 | 68 | 573 | 68.6 | 119 | 20.8 |
| 4 | 50 | 45 | 189 | 72.4 | 168.7 | 21 |
| 5 | 20 | 170 | 493 | 88.8 | 415.3 | 18.1 |

锯齿：
| 档 | 题数 | 中位数 ms | 最大 ms | 平均步数 | 平均难度分 | 平均给定数 |
|---|---|---|---|---|---|---|
| 1 | 200 | 15 | 105 | 58.5 | 77.9 | 22.5 |
| 2 | 200 | 21 | 115 | 69.8 | 118.9 | 20.3 |
| 3 | 200 | 86 | 872 | 81.2 | 167.3 | 18.9 |
| 4 | 50 | 45 | 212 | 82.7 | 203.3 | 18.9 |
| 5 | 20 | 447 | 1687 | 98 | 421.7 | 17 |

- 4 倍降速（Chromium，16 题/档）中位数 / 最大 ms：对角线 36/64、83/295、242/1167、163/441、732/2544；锯齿 43/51、62/104、185/864、147/1012、1506/5175。
  → 1–3 档中位数都远低于 1 秒，但对角线 3 档尾部 1.17s 略超目标；靠题池（每模式每档预备 2 道）让玩家通常无需等待。

## M4 结果（2026-09-23）
- 出题：按档位的笼大小分布生成笼 → 不唯一时用“两个解的差异格”重新分笼（最多 40 次；每次唯一性检查限 1500 节点）→ 实在分不开才在差异格补给定数
  → 用 ≤目标档技巧解，卡住才补正确给定数（上限：1 档 30、2 档 20、3 档 12、4/5 档 3）→ 低于目标档时再去数 → 最终评级必须等于目标档。
- 1–2 档杀手基础推理：笼的唯一组合 + 组合与已填数直接排除（都在 1 档）。
- 求解器 A：笼组合可行性传播、必含数字、二格笼精确配对、45 法则内格虚拟笼。
- 求解器 B（测试用）：杀手改用独立的“整笼填法作为精确覆盖行”的 DLX 建模；另有 30 组随机笼布局（含多解/无解）上 A、B 解数一致的单元测试。
- 组合助手：本局首次显示计 1 次辅助；说明页有 2–5 格组合速查表（程序枚举）与全部 7 种杀手技巧的真实示例。
- 完整样本 670 道全部通过（结构：笼连通、笼内不重复、笼和=答案之和、恰好覆盖 81 格）：

| 档 | 题数 | 中位数 ms | 最大 ms | 平均步数 | 平均难度分 | 平均给定数 |
|---|---|---|---|---|---|---|
| 1 | 200 | 17 | 192 | 114.1 | 202.7 | 3.9 |
| 2 | 200 | 48 | 342 | 122.3 | 230.2 | 3.5 |
| 3 | 200 | 90 | 2239 | 157.4 | 454.2 | 1.9 |
| 4 | 50 | 363 | 2474 | 172.9 | 610.2 | 1.2 |
| 5 | 20 | 719 | 3950 | 174 | 686 | 0.6 |

- 4 倍降速（12 题/档）中位数/最大 ms：1 档 60/129，2 档 88/592，3 档 368/2024，4 档 903/5312，5 档 2709/5589。
  → 3 档尾部超 1s、4 档尾部略超 5s，未完全达标；由题池预生成兜底（开局通常直接取池中题）。

## M5 结果（2026-09-23）
- 369 格整体回溯生成终盘；共享宫在几何里去重（同一组格子只算一个单元，但属于两个子盘）。
- 出题：所有档位走“唯一性挖到极小（自适应批量）→ 同一极小盘最多 6 种随机补数方案修复到目标档”；唯一性检查节点上限 1000（被中止视为不唯一）。
- 技巧跨子盘生效：鱼类在各子盘行列内；其余技巧基于通用单元与互见关系，天然跨共享宫。
- 界面：默认放大中盘；5 个缩略图（实时显示填充进度，点击切换）；共享宫角标“共享·xx”；“总览”可双指缩放/平移、只读；提示区域不在当前子盘时自动切换。
- 完整样本 180 道（1–3 档各 50、4 档 20、5 档 10）全部通过（含 4 个共享宫在两子盘中一致的检查）：

| 档 | 题数 | 中位数 ms | 最大 ms | 平均步数 | 平均难度分 | 平均给定数 |
|---|---|---|---|---|---|---|
| 1 | 50 | 410 | 3989 | 265.1 | 351.2 | 103.9 |
| 2 | 50 | 404 | 4072 | 279.6 | 403.2 | 100.3 |
| 3 | 50 | 805 | 3449 | 297.1 | 475.5 | 98.4 |
| 4 | 20 | 742 | 3717 | 306.9 | 545.9 | 97.9 |
| 5 | 10 | 1083 | 2643 | 335.3 | 971.5 | 96.5 |

- 4 倍降速（6 题/档）中位数/最大 ms：1 档 1248/2352，2 档 1262/3074，3 档 1378/9187，4 档 1512/3031，5 档 2921/8265。
  → 1–3 档中位数 <3s 达标；尾部（2、3 档）超 3s，靠题池兜底。
- 顺带修复：盘面组件 memo 导致“记笔记后不立即刷新”（新增 E2E 断言覆盖）。

## 最终自检报告（M6，2026-09-23）

### 已实现
- 五种模式全部上线且五档齐全：经典、对角线、锯齿（每局随机区域）、杀手（4–5 档基本纯杀手）、武士（369 格）。
- 本地实时出题（xoshiro128**，种子来自 crypto.getRandomValues），无题库、无固定终盘变换；唯一解；人类技巧求解器（36 种技巧）真实评级；指纹去重（换标签伪新题被识别）。
- 全部游戏功能（第 7 节）、音效（第 8 节，Web Audio 合成）、视觉与布局（第 9 节）、双平台适配（第 10 节）、完整游玩说明（第 11 节）。
- PWA：manifest/图标/apple-touch-icon、Service Worker 预缓存、断网冷启动、后台更新下次启动生效、子路径适配。
- 存档：IndexedDB 每步事务写入 + 上一份兜底 + 校验和 + 数据迁移；备份导出/导入（JSON 文件或文本码，合并式）。

### 测试的真实数字（本机 Node 22 / Playwright 1.63：WebKit 26.6、Chromium 153）
- 类型检查通过；Vitest 61/61；Playwright 功能 76 项 + 计时 11 项全部通过（3 种设备配置；4 项按设计跳过：CPU 降速只在 Chromium 跑）。
- 完整样本（FULL_SAMPLE=1，5 个模式并行跑，耗时含并行争用）：经典/对角线/锯齿/杀手各 670 道（1–3 档 200、4 档 50、5 档 20），武士 180 道（50/50/50/20/10），共 2860 道全部通过：
  独立校验器、双求解器解数=1、给定数与答案一致、技巧求解器逐步对照答案无矛盾且档位一致、指纹不重复、结构检查（杀手笼/锯齿区域/武士共享宫）。
  各模式平均步数与难度分均随档位单调上升（详表见 reports/batch-*-full.md）。
- 出题性能（浏览器主线程，Chromium）：
  - 正常 CPU：经典 1–5 档中位数 6–43ms；对角线/锯齿中位数 ≤500ms；杀手 4/5 档中位数 ~0.2–0.6s；武士 1–3 档中位数 ~0.3s。
  - 4 倍降速：经典 1–3 档最大 746ms ✓，4 档最大 261ms ✓；对角线/锯齿 1–3 档中位数 36–242ms ✓（尾部最慢 1.17s ✗）；
    杀手 1–2 档 ✓，3 档中位数 368ms（尾部 2.0s ✗），4 档中位数 903ms（尾部 5.3s ✗）；武士 1–3 档中位数 1.25–1.38s ✓（尾部最慢 9.2s ✗）。
  - 未达标的尾部靠“题池”兜底：前台空闲时每模式每档预生成 2 道，开局优先取用（E2E 已验证取用后作废、耗时 <8s）。
- 交互（4 倍降速）：按下数字键到画面出现，中位数 37–51ms，P90 ≤ 77ms；填数动画帧间隔中位数 17ms（≈60fps）。
- Worker 不可用时主线程分片出题：稳态 P99 心跳间隔 15–41ms，偶发单步最长约 120ms（单个技巧搜索无法再切分）。
- 无障碍：axe（WCAG 2 A/AA）首页、对局、设置、说明页无严重/关键问题；全部文字配色对比度 ≥ 5.2:1。
- 布局：竖屏 320×658、360×800、393×852、412×915、440×956 与横屏 852×393、浅/深色截图（docs/screenshots/）；200% 缩放（CSS 视口 196px、160px）无横向溢出且触控目标 ≥48px。

### 最低支持版本
- iPhone：iOS / iPadOS 15.4 及以上的 Safari（模块 Worker 需要 15，dvh 15.4，更早版本回退 100vh）。近三年的 iPhone 都满足。
- 安卓：Chrome / Android System WebView 90 及以上（构建目标 chrome90）；华为/小米/OPPO/vivo 等国产浏览器内核版本 ≥ 90 一般可用，但“添加到桌面”行为不一，推荐 Chrome 安装。
- 微信 / QQ 内置浏览器：可打开但会提示“请点右上角在浏览器中打开”。

### 未完全达标 / 已知限制（如实说明）
1. 出题耗时尾部：见上，对角线 3 档、杀手 3–4 档、武士 2–3 档在 4 倍降速下偶有超出目标；中位数都达标，依赖题池兜底。
2. 主线程降级模式偶有单步 >16ms（最长约 120ms）；实际目标设备都支持 Worker，此模式基本不会启用。
3. 隐性四数组、水母在随机题中几乎从不成为“下一步”（更简单技巧总能先解决），说明页的这两个示例是在真实盘面上直接演示该技巧（已注明）。
4. 用户按系统返回键后 <50ms 内立刻点开新弹层，极少数情况下新弹层会被关掉（App 自己发起的返回已修复）。
5. 音效、震动、真实安装、系统静音键、国产浏览器行为无法在云端自动化验证，需要真机检查（docs/CHECKLIST.md）。
6. 本机 WebKit 从 docker 镜像提取；CI 上直接安装（CI 全绿）。

## M7 安卓 APK（2026-09-23）
- `.github/workflows/android.yml`：Capacitor 8 生成安卓工程（`scripts/android-prepare.sh`，不入库）→ 有签名 Secret 则构建正式版并用固定密钥签名、发布到 Releases；没有则只出调试版 APK（Actions 产物）。
  PR 上的调试构建已在 GitHub Actions 实跑成功（run #3，约 3.5 分钟）。
- `.github/workflows/android-keystore.yml`：一次性在云端生成签名密钥并直接写入仓库 Secrets（不打印），已有密钥时拒绝覆盖；同时产出用备份口令加密的密钥备份。**此工作流需要老大先添加令牌，我无法替你端到端运行。**
- APK 内：不注册 SW、不显示安装引导、备份改为文本码（WebView 不支持下载文件）；有 E2E 模拟验证。appId `io.github.bencongtang673beep.sudoku`（发布后不能改）。

### 老大在手机上开启正式版 APK 的步骤（用手机浏览器打开 github.com，切“桌面版网站”更方便）
1. 头像 → Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token：
   Repository access 选 Only select repositories → openai-quota-watch；Permissions → Repository permissions → **Secrets: Read and write**；生成后复制令牌。
2. 打开仓库 → Settings → Secrets and variables → Actions → New repository secret，添加两项：
   `GH_ADMIN_TOKEN` = 刚才的令牌；`KEY_BACKUP_PASSPHRASE` = 你自己记得住的口令（至少 8 位）。
3. （PR 合并到 main 之后）仓库 → Actions → 「生成安卓签名密钥（只需运行一次）」→ Run workflow。成功后下载产物 `keystore-backup-encrypted` 存到网盘/iCloud，口令记牢——**丢了密钥以后就不能覆盖升级**。
4. Actions → 「安卓 APK」→ Run workflow → 完成后在仓库 Releases 下载 `sudoku.apk` 安装。以后每次想出新版就再运行一次（或推送 v 开头的标签）。
5. 密钥生成完即可删除 `GH_ADMIN_TOKEN` Secret 并在 GitHub 里撤销该令牌。

## 下一步
- 等老大在手机上按 docs/CHECKLIST.md 验收；有问题直接说现象，我来修。
