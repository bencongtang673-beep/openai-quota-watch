// 说明页文案：模式规则（以 docs/SPEC.md 第 3 节为准）、难度与技巧定义。
import type { TechId } from '../../engine/human/state';
import type { Level, Mode } from '../../engine/types';

export const LEVEL_TECH_SUMMARY: Record<Level, string> = {
  1: '最后一格、显性唯一（唯余）、隐性唯一（摒除）',
  2: '＋ 区块摒除',
  3: '＋ 显性/隐性数对、三数组',
  4: '＋ 四数组、X-Wing、剑鱼、XY/XYZ-Wing、摩天楼、双线风筝、简单染色、空矩形',
  5: '＋ 水母、W-Wing、X/XY-Chain、AIC、唯一矩形、BUG+1',
};

export const KILLER_LEVEL_EXTRA: Record<Level, string> = {
  1: '笼的唯一组合、组合与已填数的直接排除',
  2: '（杀手基础推理同 1 档）＋ 区块摒除',
  3: '45 法则（单个行/列/宫）、组合与候选交叉排除、笼内锁定数字',
  4: '45 法则（多格内格/外格）',
  5: '多笼联立 45 法则、组合分析与链类技巧结合',
};

export const STANDARD_RULE = '每行、每列、每宫（3×3）各含 1–9 各一次。';

export const MODE_RULES: Record<Mode, string[]> = {
  classic: ['9×9 盘面，分成 9 个 3×3 宫。', '标准规则：' + STANDARD_RULE],
  diagonal: ['标准规则：' + STANDARD_RULE, '另外，两条主对角线（左上→右下、右上→左下）也各含 1–9，不能重复。'],
  jigsaw: [
    '9×9 盘面，用 9 个形状不规则的区域代替 3×3 宫：每个区域 9 格，上下左右相连（粗线围起来的部分）。',
    '每行、每列、每个区域各含 1–9 各一次。',
    '区域布局每局随机生成，且保证有合法的答案。',
  ],
  killer: [
    '标准规则：' + STANDARD_RULE,
    '盘面被虚线“笼”完全覆盖，每格恰好属于一个笼。',
    '笼内数字之和等于笼左上角标注的数。',
    '同一笼内数字不得重复。',
  ],
  samurai: [
    '5 个 9×9 盘呈 X 形重叠：中间盘的 4 个角宫分别与 4 个角盘的内角宫共享，共 369 格。',
    '每个 9×9 盘各自满足标准规则：' + STANDARD_RULE,
    '行、列只在各自的 9×9 盘内有效——横跨 21 格的整行并不是约束单元。',
  ],
};

export const TECH_DEFS: Record<TechId, string> = {
  full_house: '某行、列或宫（区域）只剩一个空格时，缺的那个数字就是它。',
  hidden_single: '在某个单元里，某个数字只剩一个位置可以放（其他位置都被排除了），它就必须放在那里。',
  naked_single: '某格排除掉同行、同列、同宫已有的数字后，只剩一个候选，这格就填它。',
  cage_unique: '杀手数独中，有些“格数 + 和”只有一种组合，例如 2 格和为 3 只能是 {1,2}，2 格和为 17 只能是 {8,9}。笼内各格只能取这些数字。',
  pointing: '某宫里某数字的所有候选都在同一行（或列）上，那么这一行（列）在宫外的格子都不能是这个数字。',
  claiming: '某行（或列）里某数字的所有候选都落在同一个宫里，那么这个宫里其他格子都不能是这个数字。',
  cage_combo: '列出笼的所有组合，去掉与笼内已填数、或与同行/列/宫已填数矛盾的组合，剩下组合用不到的数字可从笼中删去。',
  naked_pair: '同一单元里两格的候选都只有同样的两个数字，这两个数字必定占据这两格，单元内其他格可删去它们。',
  hidden_pair: '同一单元里两个数字只出现在同样的两格中，这两格只能填这两个数字，其余候选可删。',
  naked_triple: '同一单元里三格的候选合起来只有三个数字，这三个数字必定占据这三格，单元内其他格可删去它们。',
  hidden_triple: '同一单元里三个数字只出现在同样的三格中，这三格的其他候选可删。',
  cage_cross: '把笼的组合与各格当前候选一起检验，只有真能放进去的组合才保留，据此缩小每格候选。',
  cage_locked: '若某数字出现在笼的每一种可行组合里，它就一定在笼中；如果笼里能放它的格都在同一行/列/宫，该单元的笼外格就不能是它。',
  rule45: '45 法则：一行、一列或一宫的数字之和是 45。减去完全落在其中的笼之和，就得到“伸出去的笼留在里面的格”（内格）或“伸进来的笼在外面的格”（外格）之和；只剩一格时可直接求出。',
  naked_quad: '同一单元里四格的候选合起来只有四个数字，单元内其他格可删去这四个数字。',
  hidden_quad: '同一单元里四个数字只出现在同样的四格中，这四格的其他候选可删。',
  x_wing: '某数字在两行中都只出现在相同的两列（或两列中都只在相同的两行），这两列（行）的其他格就不能是它。',
  swordfish: 'X-Wing 的三线版本：某数字在三行中只出现在相同的三列里，这三列的其他格可删去该数字。',
  xy_wing: '枢纽格候选 XY，能看到两翼 XZ 与 YZ。无论枢纽取 X 还是 Y，两翼之一必为 Z，所以同时看到两翼的格不能是 Z。',
  xyz_wing: '枢纽格候选 XYZ，两翼 XZ、YZ 都能被枢纽看到。三格中必有一个是 Z，同时看到三格的格不能是 Z。',
  skyscraper: '某数字在两条平行线上各只剩两个位置，其中一端在同一条垂直线上；另外两端至少一个是该数字，同时看到这两端的格可删去它。',
  two_string_kite: '某数字在一行、一列中各只剩两个位置，行和列各有一端落在同一宫；另两端至少一个是该数字，同时看到它们的格可删去它。',
  simple_coloring: '沿某数字的强链（单元中仅剩两个位置）把格子交替染成两色：同色格要么全是、要么全不是。同色互见则该色为假；看到两种颜色的格可删。',
  empty_rectangle: '某宫里某数字的候选全部落在一行一列组成的“十字”上，再配合宫外的一条强链，可以删去强链另一端与十字交点处的候选。',
  rule45_multi: '45 法则的推广：内格或外格有 2–4 格时，它们的和确定，结合候选可以缩小每格的取值。',
  jellyfish: 'X-Wing 的四线版本：某数字在四行中只出现在相同的四列里，这四列的其他格可删去该数字。',
  w_wing: '两个候选都是 XY 的格互不相见，若数字 X 的一条强链两端分别能看到它们，则两格至少一个是 Y，同时看到两格的格不能是 Y。',
  x_chain: '只用单一数字的交替链：强链（两者至少一真）与弱链（两者不能同真）交替相连。两端至少一个成立，同时看到两端的格可删去该数字。',
  xy_chain: '由双值格组成的链：格内两个候选构成强链，同数字互见构成弱链。两端是同一数字时，同时看到两端的格可删去它。',
  aic: '交替推理链：任意强链与弱链交替组合（可跨数字、跨格）。两端至少一个成立，据此删除与两端都冲突的候选。杀手数独中还可利用二格笼的“A=d ⇒ B=和−d”关系。',
  ur1: '唯一矩形 1 型：四格构成只占两宫的矩形且都含 {a,b}，其中三格只有 {a,b}。若第四格也取 a 或 b 就会有两个解，所以第四格删去 a、b。',
  ur2: '唯一矩形 2 型：矩形中两格只有 {a,b}，另两格同为 {a,b,x}。为避免双解，这两格之一必为 x，同时看到它们的格可删 x。',
  ur3: '唯一矩形 3 型：屋顶两格的额外候选看成一个“虚拟格”，它与同单元其他格组成显性数组，从而删除该单元其他格的候选。',
  ur4: '唯一矩形 4 型：屋顶两格所在单元中，a 只能在这两格里，所以其中之一必是 a；为避免双解，这两格都不能是 b。',
  bug1: 'BUG+1：除一格有三个候选外，所有空格都只剩两个候选。若那格不取“出现三次”的那个数，全盘将变成至少两解的 BUG 状态，与唯一解矛盾。',
  rule45_union: '把相邻的两三行（列、宫）当作一个整体使用 45 法则（总和 90 或 135），可以求出跨多个笼的内格/外格之和。',
};

export const PROBED_ONLY: TechId[] = ['hidden_quad', 'jellyfish'];
